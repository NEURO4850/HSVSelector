cmake_minimum_required(VERSION 2.8)
project( HSVSelector )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( HSVSelector HSVSelector.cpp )
target_link_libraries( HSVSelector ${OpenCV_LIBS} )